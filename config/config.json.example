{
  "check_interval": 300,

  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // Webhook中心設定（推奨: v1.2.0以降）
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // 1つのWebhookで複数チャンネルを監視する場合に便利です。
  //
  // 【使用例】
  // "webhooks": [
  //   {
  //     "name": "メインサーバー",              // Webhook識別用の名前（ログに表示）
  //     "url": "https://discord.com/api/webhooks/...",
  //     "mention": "@everyone",
  //     "channels": ["UCxxx...", "UCyyy..."]   // 監視対象チャンネルID配列
  //   },
  //   {
  //     "name": "サブサーバー",
  //     "url": "https://discord.com/api/webhooks/...",
  //     "mention": "<@&1234567890>",
  //     "channels": ["UCzzz...", "UCwww..."]
  //   }
  // ]
  //
  // 【重複監視】
  // - 同じチャンネルを複数Webhookで監視可能
  // - 例: UCxxxを「メインサーバー」と「サブサーバー」の両方で通知
  //
  // 【チャンネル個別設定との混在】
  // - この設定と channels[].webhooks を同時に使用可能
  // - 同じチャンネルに対するwebhookは自動的にマージされます
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  "webhooks": [
    {
      "name": "メインサーバー",
      "url": "https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN",
      "mention": "@everyone",
      "channels": []  // 監視したいチャンネルIDを追加（例: ["UCxxx...", "UCyyy..."]）
    }
  ],

  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // チャンネル設定
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // 監視対象のYouTubeチャンネルを設定します。
  //
  // 【チャンネルの追加方法】
  // 1. YouTubeチャンネルページのURLから「UC」で始まるチャンネルIDを取得
  //    例: https://www.youtube.com/@username → チャンネルページで右クリック「ソースを表示」
  //        → "channelId":"UCxxxxxx..." を探す
  //
  // 2. 以下のフォーマットで channels 配列に追加:
  //    {
  //      "id": "UCxxxxxxxxxxxxxxxxxxxxxx",     // チャンネルID (必須)
  //      "name": "配信者名",                    // 表示用の名前 (任意)
  //      "webhooks": [...]                      // 通知先Webhook (下記参照、オプション)
  //    }
  //
  // 3. 複数チャンネルを監視する場合はカンマ区切りで追加:
  //    "channels": [
  //      { "id": "UC111...", "name": "配信者A" },
  //      { "id": "UC222...", "name": "配信者B" },
  //      { "id": "UC333...", "name": "配信者C" }
  //    ]
  //
  // 【注意】
  // - 上記の "webhooks" セクションを使用する場合、個別の "webhooks" 設定は不要です
  // - チャンネルごとに異なるWebhookを使う場合のみ、下記の "webhooks" を設定してください
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  "channels": [
    {
      "id": "UCxxxxxxxxxxxxxxxxxxxxxx",
      "name": "配信者名",

      // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
      // チャンネル個別のWebhook設定（旧形式: 引き続きサポート）
      // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
      // このチャンネルのみ個別のWebhookを使用する場合に設定します。
      // 上記の "webhooks" セクションと併用可能（両方のwebhookに通知されます）
      //
      // 【Webhookの取得方法】
      // 1. Discordサーバーの通知先チャンネルを開く
      // 2. チャンネル設定 → 連携サービス → ウェブフック → 新しいウェブフック
      // 3. 「ウェブフックURLをコピー」をクリック
      //
      // 【Webhookの追加方法】
      // - 単一Webhook:
      //   "webhooks": [
      //     {
      //       "url": "https://discord.com/api/webhooks/123.../abc...",
      //       "mention": "@everyone"
      //     }
      //   ]
      //
      // - 複数Webhook（異なるチャンネルに同時通知）:
      //   "webhooks": [
      //     {
      //       "url": "https://discord.com/api/webhooks/111.../aaa...",
      //       "mention": "@everyone"
      //     },
      //     {
      //       "url": "https://discord.com/api/webhooks/222.../bbb...",
      //       "mention": "<@&1234567890>"
      //     },
      //     {
      //       "url": "https://discord.com/api/webhooks/333.../ccc...",
      //       "mention": ""
      //     }
      //   ]
      //
      // 【メンション設定】
      // - @everyone: 全員にメンション
      // - <@&ロールID>: 特定ロールにメンション
      //   ※ロールIDの取得: Discord開発者モードON → ロールを右クリック → IDをコピー
      // - "": メンションなし
      // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

      "webhooks": []  // 個別webhookがない場合は空配列（上記の "webhooks" セクションを使用）
    }
  ],

  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // 通知設定
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  "notification": {
    "show_thumbnail": true,              // サムネイル画像を表示
    "color": 16711680,                   // 埋め込みの色 (10進数カラーコード、赤=16711680)
    "include_end_notification": false    // 配信終了通知も送信するか
  },

  "log_level": "INFO"  // ログレベル: DEBUG, INFO, WARNING, ERROR
}
